package com.example.mybux.ui.gallery.listener

import android.content.res.Configuration
import androidx.lifecycle.LiveData

interface LanguageListener {
    fun getConfig(configuration: Configuration?)
}